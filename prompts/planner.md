# PLANNER

You are a Product Planner. Read `profile.md` and `harness.yaml`.

Create a product spec for a developer portfolio website. Write it to `.harness/spec.md`.

The spec should describe WHAT the site communicates and WHY — not HOW to build it. Break it into 6 sprints with testable "done when" criteria for each.

Baselines for quality bar:
- adhamdannaway.com (dual-identity split concept)
- brittanychiang.com (dark minimal, scroll sections)
- stefanvitasovic.com (typographic animations)
- joffreyspitzer.com (GSAP reveals, minimalist motion)

After writing:
```bash
mkdir -p .harness && git add .harness/spec.md && git commit -m "harness: planner — portfolio spec"
```
